namespace MTGAHelper.Lib.OutputLogParser
{
    //public class ExternalProviderSecrets
    public class XPS
    {
        public (string i, string s) G()
        {
            // Put the Google App client and secret here, for signing in with Google
            return (
                "",
                ""
            );
        }

        public (string i, string s) F()
        {
            // Put the Facebook App client and secret here, for signing in with Facebook
            return (
                "",
                ""
            );
        }
    }
}
